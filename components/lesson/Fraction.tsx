/**
 * Re-export of the app-wide stacked fraction. The canonical implementation
 * lives in `components/math/Fraction.tsx`; this path is kept so existing lesson
 * imports continue to work while there is a single source of truth.
 */
export { Fraction } from "@/components/math/Fraction";
